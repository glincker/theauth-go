services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: theauth
      POSTGRES_PASSWORD: theauth
      POSTGRES_DB: theauth_oauth
    ports:
      - "5436:5432"
    volumes:
      - theauth_oauth_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U theauth"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  theauth_oauth_pgdata:
