// Package provider is the namespace root for theauth-go's OAuth 2.0
// implementations. It carries no code itself; the concrete providers
// live under provider/github, provider/google, provider/microsoft, and
// provider/discord and are opt-in (import only what you use).
package provider
